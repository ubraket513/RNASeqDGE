# Source staging environment: upstream wrapper dependencies include Python.
# Extract compute payload with tools/stage_native_runtime.sh (see README).
name: rnaseqdge-alignment
channels:
  - conda-forge
  - bioconda
dependencies:
  - hisat2=2.2.1
  - subread=2.0.6
  - samtools=1.17
  - star=2.7.10b
