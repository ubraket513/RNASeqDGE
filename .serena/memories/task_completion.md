- Native C++ gate: `make -j2 check` runs vendor checks, doctest and CLI integration offline. Current P2 completion evidence: 34 cases/215 assertions; canonical and mixed-format count adapters match hand-derived P0 oracle. `make -j2 sanitize` passes ASan/UBSan/leak and both integrations. Optional retained evidence: `bash tests/integration/check_p2_retained.sh build/rnaseq`.
- P4 final: native36cases/222assertions; eight no-network STAR/HISAT2 SE/PE cases and isolated R/tool preflight PASS. P5 complete with scoped review PASS: snapshot/resume/locks/local/Slurm protocol, exact isolated R/package preflight and supervised subprocess cleanup. Final make check and real P0 alignment/merge gate PASS, actual tiny-data R failure retained. Live Slurm untested. P6 user requests local8GB/8CPU/500GB/30min reduced check:6x50kprefixreads, NC_000022.11 only,2threads/1Rworker,3repeats/backend. Published counts full36=12DEGs,withoutC28=15 with optimizer warnings; subset6 independent oracle PASS. Full-genome comparison remains outside reduced scope.
- Treat docs/IMPLEMENTATION_PROGRESS.md as current evidence; docs/SESSION_HANDOFF.md contains historical sections. Keep observed legacy failures, hand-derived fixtures, retained tool output, and scientific parity claims distinct.
- P3 gate: `.deps/p0/bin/Rscript --vanilla tests/integration/check_p3_r.R` runs an independent same-environment DESeq2/apeglm oracle, workers 1/2/4 parity, malformed/model cases, output preservation, and no-/one-DEG paths. Pair it with `make -j2 check`, `git diff --check`, and the offline-source scan documented in progress. This is synthetic parity only; P6 still needs real subset/full comparison. Do not report README runtime estimates as measured benchmarks.
