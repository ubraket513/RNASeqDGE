- Active production workflow remains pipeline.smk, launched by run_pipeline.sh which generates/submits run_job.sbatch; deprecated/ is historical. Native migration status and continuation are in docs/IMPLEMENTATION_PROGRESS.md, with decisions in docs/IMPLEMENTATION_PLAN.md and docs/DATA_CONTRACT_V1.md. P0–P5 are complete locally. P6 reduced local real-data comparison completed in27m15s: user chose8GB/8CPU/500GB and30min; six samples, chr22,50k reads/sample. A10k thinning failed normalization, so original50kSTAR resumed with2localworkers. Both backends completed realR/plots; counts877/985, correlation0.96209,zeroDEGs both. Single comparison only; full-genome/repeated benchmark and live Slurm remain unvalidated. P7 not started. User explicitly authorized continuing P5 and P6. GSE80336 GEO metadata identifies PRJNA318642/SRP073382; the old README BioProject example is mismatched. Read progress before resuming; earlier handoff sections are historical.
- P3 entry point is run_deg_analysis_offline.R over canonical TSVs. It preserves exact sample/gene identity, uses explicit directional contrasts and apeglm coefficients, emits auditable tables/plots per contrast, and atomically publishes only to absent output paths. P5 owns workflow integration; legacy run_deg_analysis.R remains active.
- Native P2 supports streamed canonical TSV, headerless legacy, and featureCounts adapters; requires explicit gene universe/run input mapping, preserves sample order/gene IDs, and aggregates technical runs with checked uint64. Keep annotation separate. See tests/fixtures/p2/README.md.
- Legacy merge maps SRR to GSM and converts reference gene identifiers through BioMart before DESeq2. Native direction preserves reference gene IDs and explicit run/sample mapping. Keep HISAT2 default until comparison gate; R/DESeq2 retained.
- Stack constraints: `mem:tech_stack`. Execution caveats: `mem:suggested_commands`. Data contracts: `mem:conventions`. Validation limits: `mem:task_completion`.
