- Supported entry point: bash run_pipeline.sh plan|local|submit CONFIG.tsv -> build/rnaseq workflow commands. Python/Snakemake legacy retired; recovery instructions docs/LEGACY_RECOVERY.md. Current evidence/status docs/IMPLEMENTATION_PROGRESS.md and docs/P7_RUNTIME.md; earlier handoff sections are historical.
- Compute prefixes .deps/runtime-r (pinned R/DESeq2/apeglm, no Python) and .deps/runtime-tools (native ELF tools plus STAR Bash dispatcher). .deps/p0 and .deps/alignment are historical/source staging prefixes, not compute PATH. HISAT2 small-index binaries called directly with --wrapper basic-0; uncompressed FASTQ and small .ht2 contract. Keep HISAT2 default.
- Canonical TSVs preserve sample/gene identities, aggregate technical runs with checked uint64, keep annotation separate. Explicit gene universe and input mapping required. See docs/DATA_CONTRACT_V1.md and tests/fixtures/p2/README.md.
- run_deg_analysis_offline.R retains DESeq2/apeglm method and publishes atomically to absent output. Compatible contrasts share models only for identical validated model data and matrices within invocation; reverse baseline refits. R workers separate from local alignment concurrency; memory estimates are reservations, not RSS caps.
- GSE80336 maps to PRJNA318642/SRP073382. P6 evidence is reduced chr22/six50kread functional comparison; whole-genome/repeated performance and live Slurm remain unvalidated. No statistical shortcuts for sparse checks. Read docs/P6_REPORT_DATA.md before interpreting results.
- Stack constraints: `mem:tech_stack`. Execution caveats: `mem:suggested_commands`. Data contracts: `mem:conventions`. Validation limits: `mem:task_completion`.